#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""m5：由 v0.3 生成 v1.0（冻结版）"""
import os, json, shutil
BASE = "/Users/taozhu/my researches/retyping"
src = f"{BASE}/方向F_预注册计划_v0.3.md"
dst = f"{BASE}/方向F_预注册计划_v1.0.md"
s = open(src, encoding="utf-8").read()
G2R = json.load(open("/tmp/gyn_retyping/g2_refined.json"))
orig = s

def rep(old, new, must=True):
    global s
    if old not in s:
        if must: raise SystemExit(f"锚点未找到: {old[:70]}")
        return
    s = s.replace(old, new, 1)

# 1) 标题与状态
rep("# 方向F 预注册计划 v0.3", "# 方向F 预注册计划 v1.0（**已冻结**）")
rep("> 日期：2026-09-11 ｜ 状态：**预注册草案（未冻结）**",
    "> 日期：2026-09-12 ｜ 状态：**✅ 已冻结（FROZEN）——阶段 1 收尾完成**")

# 2) 上游依据与 v1.0 变更说明
rep("> 上游依据：《进展与选题论证(v2)》、《四方团队审查裁决报告》、《外部队列数据获取清单_G0》、《外部队列获取执行报告_S1S5》、《跨队列对齐测试报告_v2》、**《对齐策略验证与EEEC获取报告》、**《多组学整合基准报告》**",
    "> 上游依据：《进展与选题论证(v2)》、《四方团队审查裁决报告》、《外部队列数据获取清单_G0》、《外部队列获取执行报告_S1S5》、《跨队列对齐测试报告_v2》、《对齐策略验证与EEEC获取报告》、《多组学整合基准报告》（含 §10 PAC、§11 SNF 更正）、《**R 侧方法复现与跨实现一致性报告**》、《**EEEC 批次测试床：逐基因校正的边界**》、《**EEEC 与 CPTAC 跨平台可比性报告**》")

rep("> **v0.3 变更**：",
    "> **v1.0 变更（冻结依据）**：① 新增 **F-24~F-54**（v0.3 之后的 PAC、R 侧方法、SNF 更正、批次数列、跨平台可比性共 31 条实测事实）；② **G2 阈值由效应量分布反推并冻结**（F-55~F-58；**+10% 占位值判为统计上不可实施 → 冻结为 +46.6%**，并写入「不可判条款」）；③ **V4（EEEC）由「◐ 条件性」升级为「✅ Δ 层外部平台验证队列」**（F-50）；④ 新叙事重心确认为「**层数负边际价值 + 跨队列方法选择陷阱 + 协方差层脆弱性**」。\n>\n> **v0.3 变更（保留留痕）**：")

# 3) 在 F-54 后追加 F-55~F-58
g2s = G2R["sd_relevant"]
rep("→ **需 PDC000439/441/443/445 原始矩阵（本网络受封锁）** |",
    "→ **需 PDC000439/441/443/445 原始矩阵（本网络受封锁）** |\n"
    "| **F-55** | ⚠️ **现有方法排序在统计上不成立** | 与最佳基线（`KMeans(concat)` = **0.4027**）的**配对 t 检验**：**共联共识 均值差 −0.0196、p = 0.669（不显著）**；谱嵌入 −0.1619（p=4.5×10⁻⁵）、MCCA/MOFA-lite −0.1920（p=3.6×10⁻⁸）、PCA −0.2234（p=9.1×10⁻⁹）、NMF −0.2205（p=1.8×10⁻⁹）。→ **只有第 3 名及以后与基线显著分离；「第 1 名」不可宣称** |\n"
    "| **F-56** | **冠军随留出队列翻转率 50.0%** | 22 个 (域×层子集) 单元中 **11 个（50.0%）的冠军不一致**，平均每单元 **1.77** 个不同冠军。按均值排序 KMeans 第一（0.4027）而按夺冠次数 共联共识 第一（19 vs 16）——**「最佳方法」本身不稳定** |\n"
    "| **F-57** | **三个噪声来源与选择偏倚已量化** | ① 留出队列选择：同一 (域,层子集,方法) 跨队列对的 ARI **SD = 0.0975**（R 侧独立复核 0.1293）；② K 选择（3/4/5）：**SD = 0.0643**；③ 方法配对差：全方法对 **SD = 0.2795**、与最佳基线的**决策相关 SD = 0.3146**；④ **max-of-8 选择偏倚 ≈ 0.1387** |\n"
    f"| **F-58** | ⚠️ **G2 的 +10% 占位值统计上不可实施（反推结果）** | 在 n=22、α=0.05、power=0.8 下由决策相关配对标 SD（{g2s:.4f}）反推：**MDD = 0.1878 → 相对阈值 +46.6%（绝对目标 0.5905）**。**检测 +10% 需 n = 479**（全方法对噪声下需 378）；**功率模拟：真提升 +10% 在 n=22 上仅 13.7% 概率被发现**（+30% 也仅 54.0%），而真无提升时假阳性率 5.0% —— **两者几乎无法区分**。n 敏感性：n=44 → +33.0%；n=66 → +26.9% |")

# 4) G2 定义冻结
rep("| **G2** | 阶段 2 | 新算法在留出队列的 ARI ≥ 最佳基线 **+10%**（**+10% 为占位值，须由效应量分布反推后在 v1.0 冻结**） | ⏳ 待判；不过门则降级为\"基准 + 指南 + 负面结果\" |",
    "| **G2**（**v1.0 冻结**） | 阶段 2 | **基线冻结**为单一方法 `KMeans(逐层标准化后拼接)` = **0.4027**（**禁止在评估集上重挑基线**，否则引入 F-57 的 0.1387 选择偏倚）；**评估规模冻结** n = 22 个 (域×层子集) 单元、留出队列固定；**阈值 = 噪声反推 MDD = +46.6%**（**绝对目标 0.5905**；α=0.05 单侧、power=0.8、决策相关配对标 SD 0.3146）。**「不可判条款」**：若真实提升 < +15%，n=22 下 power < 21%（F-58），此时记为**「不可判（under-powered）」而非「未通过」**；若要保留 +10% 语义，须先把评估规模扩至 **n ≥ 479** | ⏳ 待判（判据已冻结）；不过门则降级为\"基准 + 指南 + 负面结果\" |")

# 5) P0 行
rep("| P0 资产盘点与预注册 | 2–3 | 数据字典、可用性矩阵、样本去重报告、预注册 v1.0 冻结 | G0 | ✅ 完成（v0.3 待冻结为 v1.0） |",
    "| P0 资产盘点与预注册 | 2–3 | 数据字典、可用性矩阵、样本去重报告、预注册 v1.0 冻结 | G0 | ✅ **完成（v1.0 已冻结：G2 阈值由效应量分布反推并落定）** |")

# 6) 版本历史
rep("| **v0.3** | 新增 **F-24~F-31**；**H6 判已证伪、H2 判已获支持**；**G1/G4 落判**；§4.2.1 冻结非循环协议；§4.3 新增样本交集约束；承诺 11–12 |",
    "| **v0.3** | 新增 **F-24~F-31**；**H6 判已证伪、H2 判已获支持**；**G1/G4 落判**；§4.2.1 冻结非循环协议；§4.3 新增样本交集约束；承诺 11–12 |\n"
    "| **v1.0（冻结）** | 合并 **F-32~F-58**（EEEC 语义解码/批次测试床/跨平台可比性/R 侧复现/PAC）；**G2 阈值反推并冻结（+46.6%，含不可判条款）**；**V4 升级为 Δ 层外部平台验证队列**；叙事重心定稿 |")

# 7) 待办勾销
rep("- [ ] 阶段 1 收尾后用效应量分布反推 G2 阈值，冻结 v1.0",
    "- [x] **阶段 1 收尾：用效应量分布反推 G2 阈值并冻结 v1.0** → ✅ 完成（F-55~F-58；阈值 **+46.6%**、绝对目标 **0.5905**；含**不可判条款**）")

assert s != orig, "无任何替换生效"
open(dst, "w", encoding="utf-8").write(s)
print(f"已生成 v1.0: {dst}")
print(f"行数 {len(s.splitlines())} | 字节 {len(s.encode())}")
for tag in ["v1.0（**已冻结**）", "F-55", "F-56", "F-57", "F-58", "+46.6%", "0.5905", "不可判条款", "V4 升级"]:
    print(f"  {tag:18s}", "✓" if tag in s else "✗ 缺")
